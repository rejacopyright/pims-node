generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["fullTextSearchPostgres", "prismaSchemaFolder", "omitApi"]
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
